name: Django_deploy

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: pull and reload
        run: sshpass -p Marvolo+-* ssh -o StrictHostKeyChecking=no root@123.57.194.168 "cd ~/academic; git pull;"
